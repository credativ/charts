apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seatsurfing.fullname" . }}-test-connection"
  labels:
    {{- include "seatsurfing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seatsurfing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
